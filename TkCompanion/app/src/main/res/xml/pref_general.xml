<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="stopwatch_mode"
        android:title="@string/pref_title_stopwatch_mode"
        android:defaultValue="1"
        android:entries="@array/pref_stopwatch_mode_titles"
        android:entryValues="@array/pref_stopwatch_mode_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <!--<CheckBoxPreference-->
        <!--android:key="stopwatch_competition"-->
        <!--android:title="@string/pref_title_stopwatch_mode"-->
        <!--android:summary="@string/pref_description_stopwatch_mode"-->
        <!--android:defaultValue="true" />-->

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:key="gap_time"
        android:title="@string/pref_title_display_gap_time"
        android:defaultValue="5"
        android:selectAllOnFocus="true"
        android:inputType="numberDecimal"
        android:singleLine="true"
        android:maxLines="1" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <!--
    <ListPreference
        android:key="example_list"
        android:title="@string/pref_title_add_friends_to_messages"
        android:defaultValue="-1"
        android:entries="@array/pref_example_list_titles"
        android:entryValues="@array/pref_example_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />
-->
</PreferenceScreen>
